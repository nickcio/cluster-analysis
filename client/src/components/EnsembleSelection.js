import React, { useContext, useState } from 'react'
import { GlobalStoreContext } from '../store'

import Box from "@mui/material/Box";

export default function EnsembleSelection() {
    return (
        <Box sx={{bgcolor: "blue"}} style={{width: "100%", height: "100%"}}>

        </Box>
    )
}